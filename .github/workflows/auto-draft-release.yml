name: Auto Release from Upstream

on:
  schedule:
    - cron: '30 0 * * *' # Run daily at 00:30 UTC
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  auto-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}

      - name: Fetch Latest Upstream Release
        id: upstream
        env:
          GH_TOKEN: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}
        run: |
          # Fetch the latest release (including pre-releases)
          LATEST_RELEASE=$(gh api repos/XTLS/Xray-core/releases | jq '.[0]')
          
          # Extract the tag name
          TAG_NAME=$(echo "$LATEST_RELEASE" | jq -r '.tag_name')
          
          # Extract the title (name)
          RELEASE_NAME=$(echo "$LATEST_RELEASE" | jq -r '.name')
          
          # Extract the prerelease status
          IS_PRERELEASE=$(echo "$LATEST_RELEASE" | jq -r '.prerelease')
          
          # Extract the body (release notes) and save to a file to handle multiline
          echo "$LATEST_RELEASE" | jq -r '.body' > release_notes.txt
          
          echo "tag=$TAG_NAME" >> "$GITHUB_OUTPUT"
          echo "name=$RELEASE_NAME" >> "$GITHUB_OUTPUT"
          echo "prerelease=$IS_PRERELEASE" >> "$GITHUB_OUTPUT"
          
          echo "Upstream latest release is $TAG_NAME (Prerelease: $IS_PRERELEASE)"

      - name: Check Local Releases
        id: local
        env:
          GH_TOKEN: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ steps.upstream.outputs.tag }}"
          
          # Check if the release already exists locally
          if gh release view "$TAG_NAME" > /dev/null 2>&1; then
            echo "Release $TAG_NAME already exists in this repository."
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Release $TAG_NAME not found."
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish New Release
        if: steps.local.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ secrets.PAT || secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="${{ steps.upstream.outputs.tag }}"
          RELEASE_NAME="${{ steps.upstream.outputs.name }}"
          IS_PRERELEASE="${{ steps.upstream.outputs.prerelease }}"
          
          PRERELEASE_FLAG=""
          if [ "$IS_PRERELEASE" = "true" ]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          
          echo "Publishing release $TAG_NAME (Prerelease: $IS_PRERELEASE)..."
          
          gh release create "$TAG_NAME" \
            $PRERELEASE_FLAG \
            --title "$RELEASE_NAME" \
            --notes-file release_notes.txt \
            --target main
            
          echo "Successfully published release $TAG_NAME!"
